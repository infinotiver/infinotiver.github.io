async function updateBuildInfo() { try { const t = await fetch("https://api.github.com/repos/infinotiver/infinotiver.github.io/commits?per_page=1"); if (!t.ok) throw new Error(`GitHub API ${t.status}`); const [e] = await t.json(), n = e?.sha.substring(0, 7), o = e?.commit.author.date, i = o ? new Date(o).toLocaleString("en-US", { year: "numeric", month: "short", day: "numeric", hour: "2-digit", minute: "2-digit", timeZoneName: "short" }) : null, a = document.getElementById("last-commit"), s = document.getElementById("last-update"); a && (a.textContent = n || "N/A"), s && (s.textContent = i || "N/A") } catch (t) { console.error("Error fetching GitHub data:", t) } } document.addEventListener("DOMContentLoaded", () => { const t = document.querySelector(".hero-name"), e = document.querySelectorAll(".social-links a"); if (t && e.length) { const n = t.textContent; let o; const i = e => { clearTimeout(o), t.classList.add("is-swapping"), o = setTimeout(() => { t.textContent = e, t.classList.remove("is-swapping") }, 180) }; e.forEach(t => { t.addEventListener("mouseenter", () => i(t.dataset.name || n)), t.addEventListener("mouseleave", () => i(n)) }) } updateBuildInfo() }), (() => { const t = Math.floor(360 * Math.random()); document.documentElement.style.setProperty("--bg", `hsl(${t}, 8%, 6%)`) })();